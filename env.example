# Database Configuration
# PostgreSQL database URL - replace with your actual database credentials
POSTGRES_URL=postgresql://username:password@localhost:5432/firstaid_db

# Authentication
# Generate a secure random string for JWT token signing
# You can use: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-jwt-key-replace-with-random-string
NEXTAUTH_URL=http://localhost:3000

# AI Provider Configuration (Ollama)
# Ollama API endpoint - default local installation
OLLAMA_BASE_URL=http://localhost:11434/v1


